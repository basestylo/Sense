apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-claim0
  labels:
    io.kompose.service: app-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
